{
  "name": "priority-board",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "concurrently -k -n backend -c red \"npm --prefix backend run dev\"",
    "dev:frontend": "concurrently -k -n frontend -c blue \"npm --prefix frontend run dev:all\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  }
}
